# services:
#   db:
#     image: mysql:8.0
#     env_file:
#       - ./.env
#     environment:
#       MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-rootpassword}
#       MYSQL_DATABASE: ${MYSQL_DATABASE:-servd}
#       MYSQL_USER: ${MYSQL_USER:-servd}
#       MYSQL_PASSWORD: ${MYSQL_PASSWORD:-servdpass}
#     ports:
#       - "${MYSQL_PORT:-3306}:3306"
#     volumes:
#       - db_data:/var/lib/mysql
#     healthcheck:
#       test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-uroot", "-p${MYSQL_ROOT_PASSWORD:-rootpassword}"]
#       interval: 10s
#       timeout: 5s
#       retries: 10

#   backend:
#     image: ghcr.io/${GHCR_REPO}/backend:latest
#     ports:
#       - "${BACKEND_PORT:-3000}:3000"
#     env_file:
#       - ./.env
#     environment:
#       DATABASE_URL: mysql://${MYSQL_USER:-servd}:${MYSQL_PASSWORD:-servdpass}@db:3306/${MYSQL_DATABASE:-servd}?connection_limit=5
#       FRONTEND_URL: ${FRONTEND_URL:-http://localhost:5173}
#     depends_on:
#       db:
#         condition: service_healthy
#     command: sh -c "npx prisma db push && npm start"

#   frontend:
#     image: ghcr.io/${GHCR_REPO}/frontend:latest
#     ports:
#       - "${FRONTEND_PORT:-5173}:80"
#     depends_on:
#       - backend

# volumes:
#   db_data:
